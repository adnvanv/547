<UserControl x:Class="VetClinic.App.Views.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Loaded="OnLoaded">
    <Grid Margin="8,16,8,8">
        <!-- Shown while administrator mode is off -->
        <StackPanel x:Name="LockedPanel" MaxWidth="520" HorizontalAlignment="Left">
            <TextBlock Text="Administration" Style="{StaticResource SectionHeader}" />
            <TextBlock TextWrapping="Wrap" Margin="0,0,0,12"
                       Text="Veterinarian staff records can only be changed by an administrator." />
            <TextBlock Style="{StaticResource Hint}" Margin="0,0,0,16"
                       Text="Turn on Administrator mode on the Settings tab to unlock this page." />
            <Button Content="Open Settings" HorizontalAlignment="Left" Click="OpenSettings_Click" />
        </StackPanel>

        <Grid x:Name="AdminPanel" Visibility="Collapsed">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="24" />
                <ColumnDefinition Width="360" />
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Column="0">
                <TextBlock DockPanel.Dock="Top" Text="Administration: Veterinarians" Style="{StaticResource SectionHeader}" />
                <TextBlock x:Name="ResultCount" DockPanel.Dock="Bottom" Margin="0,6,0,0" Style="{StaticResource Hint}" />
                <DataGrid x:Name="Grid" SelectionChanged="Grid_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding VeterinarianId}" Width="50" />
                        <DataGridTextColumn Header="First name" Binding="{Binding FirstName}" Width="*" />
                        <DataGridTextColumn Header="Last name" Binding="{Binding LastName}" Width="*" />
                        <DataGridTextColumn Header="Specialty" Binding="{Binding Specialty}" Width="1.5*" />
                        <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="70" />
                        <DataGridTextColumn Header="Appointments" Binding="{Binding AppointmentCount}" Width="110" />
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>

            <StackPanel Grid.Column="2">
                <TextBlock x:Name="FormTitle" Text="New veterinarian" Style="{StaticResource SectionHeader}" />
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition /><RowDefinition /><RowDefinition /><RowDefinition /><RowDefinition />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="ID" Style="{StaticResource FormLabel}" />
                    <TextBox Grid.Row="0" Grid.Column="1" x:Name="IdBox" IsReadOnly="True" Focusable="False" />
                    <TextBlock Grid.Row="1" Text="First name *" Style="{StaticResource FormLabel}" />
                    <TextBox Grid.Row="1" Grid.Column="1" x:Name="FirstNameBox" MaxLength="50" />
                    <TextBlock Grid.Row="2" Text="Last name *" Style="{StaticResource FormLabel}" />
                    <TextBox Grid.Row="2" Grid.Column="1" x:Name="LastNameBox" MaxLength="50" />
                    <TextBlock Grid.Row="3" Text="Specialty" Style="{StaticResource FormLabel}" />
                    <TextBox Grid.Row="3" Grid.Column="1" x:Name="SpecialtyBox" MaxLength="100" />
                    <TextBlock Grid.Row="4" Text="Status" Style="{StaticResource FormLabel}" />
                    <CheckBox Grid.Row="4" Grid.Column="1" x:Name="ActiveBox" Content="Active (can be booked)"
                              Margin="0,0,0,8" />
                </Grid>

                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <Button Content="New" Click="New_Click" />
                    <Button Content="Save" Click="Save_Click" Style="{DynamicResource AccentButtonStyle}"
                            Margin="0,0,8,0" MinWidth="90" />
                    <Button x:Name="DeleteButton" Content="Delete" Click="Delete_Click" />
                </StackPanel>
                <TextBlock Margin="0,16,0,0" Style="{StaticResource Hint}"
                           Text="A veterinarian with appointments on record cannot be deleted; mark them inactive instead." />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
